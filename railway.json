{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd server && npm install && npx prisma generate && npx prisma migrate deploy && cd ../client && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npx prisma db seed || true && NODE_ENV=production npx tsx src/index.ts",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}
